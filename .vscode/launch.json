{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run (jai)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/RenderPractice",
            "args": [],
            "preLaunchTask": "build (jai)",
            "cwd": "${workspaceFolder}"
        }
    ]
}